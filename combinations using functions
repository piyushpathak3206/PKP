#include <stdio.h>
int combination(int n,int r){
 if(n>=r){
    float j = 1;
    for (int i = 0;i<r;i++)
      j=j*(n-i)/(r-i);
    return j;}
 else
  return 0;}

int main()
{
int n,r;
printf("enter value of n and r: \n");
scanf("%d%d",&n,&r);
printf("%d",combination(n,r));

    return 0;
}
